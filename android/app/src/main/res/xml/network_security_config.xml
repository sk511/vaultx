<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Fortress Vault
  - Blocks all cleartext (HTTP) traffic
  - Only Firebase / Google APIs are allowed over HTTPS
  - No user-installed CA certs trusted (prevents MITM via custom roots)
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Only trust system CAs, not user-installed ones -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Firebase domains get full system trust -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">pwnedpasswords.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
